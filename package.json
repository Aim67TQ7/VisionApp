{
  "name": "curbappeal-ai",
  "version": "1.0.0",
  "description": "CurbAppeal AI - Evaluate and enhance curb appeal with AI-powered insights.",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "@types/node": "^14.14.31",
    "@types/react": "^17.0.3",
    "@types/react-dom": "^17.0.3",
    "axios": "^0.21.1",
    "aws-sdk": "^2.934.0",
    "dotenv": "^10.0.0",
    "jspdf": "^2.3.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.2.0",
    "stripe": "^8.174.0"
  },
  "devDependencies": {
    "@types/aws-sdk": "^2.7.0",
    "@types/jspdf": "^2.0.1",
    "@types/react-router-dom": "^5.1.7",
    "typescript": "^4.1.5"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}

// Description:
// This is the package.json file for the CurbAppeal AI project.
// It includes dependencies required for core features such as image upload (aws-sdk), PDF generation (jspdf), API integration (axios), and payment processing (stripe).
// React and react-router-dom are used for UI components, while dotenv helps manage environment variables securely.
// Dev dependencies include TypeScript and necessary type definitions for strong typing.